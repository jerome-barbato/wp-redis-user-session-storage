{

  "version"       : "0.1",
  "name"          : "metabolism/wp-redis-user-session-storage",
  "description"   : "Store WordPress user session tokens in Redis rather than the usermeta table. Requires PECL Redis library.",
  "homepage"      : "https://github.com/ethitter/WP-Redis-User-Session-Storage",
  "authors"       : [
    {
      "name": "Erick Hitter"
    }
  ],
  "type"          : "wordpress-plugin",
  "require"       : {
    "composer/installers": "~1.0"
  }
}
